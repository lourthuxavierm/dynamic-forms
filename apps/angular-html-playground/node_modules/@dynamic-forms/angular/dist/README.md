# @dynamic-forms/angular

Experimental Angular 22 signals/lifecycle adapter for `@dynamic-forms/core`.
It provides a typed form facade, readonly signals, DI providers, an Observable
event bridge, and a whole-form ControlValueAccessor directive. It does not ship
browser controls; use `@dynamic-forms/angular-html` for the experimental native
renderer.
