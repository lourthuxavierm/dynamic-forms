# Angular HTML installation

- Status: Experimental partial-Ivy package
- Owner: Angular HTML maintainers
- Last verified: 2026-08-27
- Applies to: Angular 22

```sh
pnpm add @dynamic-forms/core @dynamic-forms/angular @dynamic-forms/angular-html @angular/core @angular/common @angular/forms rxjs
```

Import `@dynamic-forms/angular-html/styles.css` for the optional default style
layer. Version 0.1.0 is distributed as an Angular partial-Ivy library, so the
standard Angular application toolchain links it for development and production
builds. The Experimental label reflects the control and compatibility scope,
not a JIT-only packaging limitation.
