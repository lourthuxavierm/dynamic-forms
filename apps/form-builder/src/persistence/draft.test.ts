import{beforeEach,describe,expect,it}from'vitest';import type{FormSchema}from'@dynamic-form-engine/core';import{createSnapshot,listForms,loadDatabase,loadDraft,publish,rollbackRelease,saveDraft}from'./draft';
const schema:FormSchema={id:'test-form',fields:[{name:'name',type:'text'}]};describe('workspace persistence',()=>{beforeEach(()=>localStorage.clear());it('migrates the legacy draft',()=>{localStorage.setItem('dynamic-forms:builder:draft',JSON.stringify({version:1,schema,selectedPath:'name'}));expect(loadDatabase().version).toBe(2);expect(loadDraft().selectedPath).toBe('name')});it('keeps releases immutable and rolls them back as drafts',()=>{saveDraft(schema,'name');createSnapshot(schema);const release=publish(schema,{sections:[]});(release.schema.fields as any[])[0].label='changed';expect(listForms()[0].releases[0].schema.fields[0].label).toBeUndefined();const restored=rollbackRelease(release.id);expect(restored?.fields[0].name).toBe('name');expect(listForms()[0].status).toBe('Draft')})});
