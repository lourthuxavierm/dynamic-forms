import{describe,expect,it}from'vitest';import type{FormSchema}from'@dynamic-form-engine/core';import{validateBuilderSchema}from'./builderValidation';import{rulesFor}from'../builder/rules/rules';
describe('large schema hardening',()=>{it.each([100,500,1000])('processes %i fields within the interactive budget',(count)=>{const schema:FormSchema={id:'large',fields:Array.from({length:count},(_,i)=>({name:`field${i}`,type:'text' as const,dependsOn:i?[`field${i-1}`]:undefined}))};const start=performance.now();expect(validateBuilderSchema(schema)).toEqual([]);expect(rulesFor(schema)).toHaveLength(count-1);expect(performance.now()-start).toBeLessThan(1000)});it('preserves unknown field types during parsing',()=>{const schema={id:'future',fields:[{name:'custom',type:'future-widget',config:{answer:42}}]} as unknown as FormSchema;expect(JSON.parse(JSON.stringify(schema))).toEqual(schema);expect(validateBuilderSchema(schema)).toEqual([])})});
