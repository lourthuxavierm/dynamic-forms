import { Box, Chip, Paper, Stack, Typography } from '@mui/material';
import type { LocationField } from '../examples/dependent/DependentExample';
const nodes: LocationField[] = ['country', 'state', 'city'];
export default function DependencyGraphView({ affected }: { affected: readonly LocationField[] }) { return <Paper variant="outlined" sx={{ p: 2 }} aria-label="Dependency graph"><Typography variant="h6">Dependency graph</Typography><Stack direction={{ xs: 'column', sm: 'row' }} alignItems={{ sm: 'center' }} spacing={1} sx={{ mt: 1 }}>{nodes.map((node, index) => <Stack key={node} direction="row" alignItems="center" spacing={1}><Box sx={{ p: 1.5, border: 2, borderColor: affected.includes(node) ? 'warning.main' : 'divider', borderRadius: 1, bgcolor: affected.includes(node) ? 'warning.light' : 'background.paper' }}><Typography>{node}</Typography>{affected.includes(node) && <Chip size="small" label="affected" color="warning" />}</Box>{index < nodes.length - 1 && <Typography aria-hidden="true">-&gt;</Typography>}</Stack>)}</Stack></Paper>; }
