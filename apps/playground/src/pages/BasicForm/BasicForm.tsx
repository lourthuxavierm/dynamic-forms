import DemoShell from '../../components/DemoShell/DemoShell';
import { basicExampleValues, basicFormSchema, basicInitialValues } from '@dynamic-forms/examples/basic';
import { basicExampleSource } from '../../examples/basic/BasicExample';
export default function BasicForm() { return <DemoShell metadata={{ title: 'Basic form', purpose: 'Edit, validate, inspect, and submit a six-control employee profile.', difficulty: 'beginner', packages: ['@dynamic-forms/core', '@dynamic-forms/react', '@dynamic-forms/mui'], expectedBehavior: ['Empty submission displays every required error and focuses the first invalid field.', 'Invalid email and age formats display schema validation messages.', 'Load example values performs a programmatic multi-field update.', 'Valid submission displays serialized data.', 'Reset restores initial values and clears interaction state.'] }} schema={basicFormSchema} defaultValues={basicInitialValues} exampleValues={basicExampleValues} source={basicExampleSource} />; }
