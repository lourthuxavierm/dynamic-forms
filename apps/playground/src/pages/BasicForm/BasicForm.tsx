import DemoShell from '../../components/DemoShell/DemoShell';
import { quickstartSchema } from '../../examples/QuickstartExample';
const source = `import { FormProvider } from '@dynamic-forms/react';\nimport { MuiForm } from '@dynamic-forms/mui';\n\n<FormProvider schema={schema} defaultValues={{ fullName: '', email: '', role: '' }}>\n  <MuiForm schema={schema} onSubmit={console.log} />\n</FormProvider>`;
export default function BasicForm() { return <DemoShell metadata={{ title: 'Basic form', purpose: 'Edit, validate, inspect, and submit a schema-driven profile form.', difficulty: 'beginner', packages: ['@dynamic-forms/core', '@dynamic-forms/react', '@dynamic-forms/mui'], expectedBehavior: ['Empty submission displays every required error and focuses the first invalid field.', 'Valid values submit as serialized data.', 'Reset restores initial values and clears interaction state.', 'An invalid schema draft reports diagnostics and preserves the last valid preview.'] }} schema={quickstartSchema} defaultValues={{ fullName: '', email: '', role: '' }} source={source} />; }
