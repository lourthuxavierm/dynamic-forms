import { Card, CardContent, Chip, Grid, Stack, Typography } from '@mui/material';
import { productGoals } from '../../config/productGoals';
export default function Dashboard() { return <><Typography variant="h3" component="h1" gutterBottom>Enterprise playground</Typography><Typography color="text.secondary" sx={{ mb: 3 }}>Executable examples with transparent maturity.</Typography><Grid container spacing={2}>{productGoals.map((goal) => <Grid key={goal.id} size={{ xs: 12, md: 6, xl: 4 }}><Card variant="outlined" sx={{ height: '100%' }}><CardContent><Stack direction="row" spacing={1} sx={{ mb: 1 }}><Chip size="small" label={goal.priority} color={goal.priority === 'P0' ? 'error' : 'default'} /><Chip size="small" label={goal.status} /></Stack><Typography variant="h6">{goal.title}</Typography><Typography variant="body2" color="text.secondary" sx={{ mt: 1 }}>{goal.evidence[0]}</Typography></CardContent></Card></Grid>)}</Grid></>; }
