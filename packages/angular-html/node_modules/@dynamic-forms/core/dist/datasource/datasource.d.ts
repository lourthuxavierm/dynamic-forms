import type { DataSource, DataSourceConfig, DataSourceContext, DataSourceResult } from './types';
export interface DataSourceLoadOptions {
    search?: string;
    page?: number;
    pageSize?: number;
    signal?: AbortSignal;
}
export interface DataSourceManagerOptions {
    fetch?: typeof fetch;
}
export declare class DataSourceManager {
    private readonly sources;
    private readonly cache;
    private readonly controllers;
    private readonly generations;
    private readonly states;
    private readonly fetchImpl;
    constructor(options?: DataSourceManagerOptions);
    register<T>(name: string, source: DataSource<T>): void;
    unregister(name: string): void;
    has(name: string): boolean;
    getState<T>(name: string): DataSourceResult<T> | undefined;
    load<T>(name: string, context: DataSourceContext): Promise<T[]>;
    loadConfig<T>(name: string, config: DataSourceConfig<T>, context: DataSourceContext, options?: DataSourceLoadOptions): Promise<T[]>;
    cancel(name: string): void;
    clearCache(): void;
    clear(): void;
    private resolveConfig;
    private loadUrl;
}
//# sourceMappingURL=datasource.d.ts.map