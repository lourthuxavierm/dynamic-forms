# @dynamic-form-engine/angular

Experimental Angular 22 signals/lifecycle adapter for `@dynamic-form-engine/core`.
It provides a typed form facade, readonly signals, DI providers, an Observable
event bridge, and a whole-form ControlValueAccessor directive. It does not ship
browser controls; use `@dynamic-form-engine/angular-html` for the experimental native
renderer.
