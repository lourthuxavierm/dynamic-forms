import type { FormStore, FormValues } from '../store';
import type { FormSchema } from '../schema';
export interface FieldConditionState {
    visible: boolean;
    disabled: boolean;
    required: boolean;
    readOnly: boolean;
}
export declare class ConditionController<T extends FormValues = FormValues> {
    private readonly store;
    private readonly onChange?;
    private readonly fields;
    private readonly states;
    private readonly dependencies;
    private readonly listeners;
    private readonly fieldListeners;
    private readonly versions;
    private readonly unsubscribers;
    private version;
    constructor(store: FormStore<T>, schema: FormSchema, onChange?: ((path: string, state: FieldConditionState) => void) | undefined);
    getState(path: string): FieldConditionState | undefined;
    getVersion(path?: string): number;
    subscribe(listener: () => void): () => void;
    subscribe(path: string, listener: () => void): () => void;
    dispose(): void;
    private getAffectedFields;
    private recalculate;
}
//# sourceMappingURL=controller.d.ts.map