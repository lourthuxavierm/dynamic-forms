import type { FormListener, FormState, FormSubmitHandler, FormValidator, FormValues, ResetOptions, SetValueOptions } from './types';
import { type FormEventListener, type FormEventType } from '../events';
export declare class FormStore<T extends FormValues = FormValues> {
    private state;
    private readonly listeners;
    private readonly fieldListeners;
    private readonly events;
    private initialValues;
    constructor(initialValues?: T);
    getState(): FormState<T>;
    getValues(): T;
    getValue(path: string): unknown;
    setValue(path: string, value: unknown, options?: SetValueOptions): void;
    setValues(values: Partial<T>, options?: SetValueOptions): void;
    setError(path: string, message: string): void;
    clearError(path: string): void;
    setTouched(path: string, touched?: boolean): void;
    setDisabled(disabled: boolean): void;
    setLoading(loading: boolean): void;
    setSubmitting(submitting: boolean): void;
    validate(validator: FormValidator<T>): Promise<boolean>;
    submit<TResult>(onSubmit: FormSubmitHandler<T, TResult>, validator?: FormValidator<T>): Promise<TResult | undefined>;
    reset(newInitialValues?: T, options?: ResetOptions): void;
    resetField(path: string): void;
    on(type: FormEventType, listener: FormEventListener): () => void;
    subscribe(listener: FormListener<T>): () => void;
    subscribeToField(path: string, listener: FormListener<T>): () => void;
    private updateState;
    private notify;
    private notifyPaths;
    private notifyAll;
}
//# sourceMappingURL=store.d.ts.map